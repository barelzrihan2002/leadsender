import { type ClassValue } from "clsx";
export declare function cn(...inputs: ClassValue[]): string;
export declare function formatPhoneNumber(phone: string): string;
export declare function getRandomDelay(min: number, max: number): number;
export declare function replaceVariables(template: string, variables: Record<string, string>): string;
