appId: com.leadsender.app
productName: LeadSender
directories:
  output: dist
  buildResources: build
files:
  - dist-electron
  - dist
  - package.json
asarUnpack:
  - node_modules/better-sqlite3/**/*
  - node_modules/whatsapp-web.js/**/*
win:
  target:
    - nsis
  icon: build/icon.ico
  artifactName: ${productName}-Setup-${version}.${ext}
  publisherName: LeadSender
  verifyUpdateCodeSignature: false
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: LeadSender
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  deleteAppDataOnUninstall: false
# Publish configuration removed - manual upload to GitHub Releases
# This keeps the source code private while still enabling auto-updates
publish:
  provider: github
  owner: barelzrihan2002
  repo: leadsender-releases
  releaseType: release
